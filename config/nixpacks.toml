[phases.setup]
nixPkgs = ["python311", "pip"]

[phases.install]
cmds = [
    "cd frontend && npm install && npm run build",
    "cd ../backend && pip install -r requirements.txt",
    "cp -r docs/help backend/docs_help"
]

[start]
cmd = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
